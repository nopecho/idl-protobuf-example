# Node Example

ref. https://github.com/bufbuild/protobuf-es

`package.json` 설정

```json
{
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.3"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.50.0"
  }
}
```
